<script>
  // Inject front matter data into page for JavaScript access
  window.pageMetadata = {
  {{#if extra.module}}
    module: "{{extra.module}}",
  {{/if}}
  {{#if extra.title}}
    moduleTitle: "{{extra.title}}",
  {{/if}}
  {{#if chapter_title}}
    chapterTitle: "{{chapter_title}}"
  {{/if}}
  };
</script>